apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim
spec:
  storageClassName: manual
  volumeName: mysql-pv-volume
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
