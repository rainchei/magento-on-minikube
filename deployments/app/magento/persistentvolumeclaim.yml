apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: magento-pv-claim
spec:
  storageClassName: manual
  volumeName: magento-pv-volume
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
